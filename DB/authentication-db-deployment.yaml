apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: authentication-db
  name: authentication-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: authentication-db
  template:
    metadata:
      labels:
        io.kompose.service: authentication-db
    spec:
      containers:
      - env:
        - name: MYSQL_DATABASE
          value: authentication
        - name: MYSQL_PASSWORD
          value: pswd
        - name: MYSQL_ROOT_PASSWORD
          value: root
        - name: MYSQL_USER
          value: user
        image: mysql:8
        imagePullPolicy: "Always"
        name: authentication-db
        ports:
        - containerPort: 3306
        resources: {}
        volumeMounts:
        - mountPath: /var/lib/mysql
          name: authentication-volume
      restartPolicy: Always
      volumes:
      - name: authentication-volume
        persistentVolumeClaim:
          claimName: authentication-volume
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: authentication-db-service
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    io.kompose.service: authentication-db
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: authentication-volume
  name: authentication-volume
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
